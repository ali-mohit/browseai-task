export type InputParameters = {
    name: string,
    label: string,
    pattern: string,
    required: boolean,
    encrypted: boolean,
    description: string,
    defaultValue: any,
    type: string,
    value: string,
    min: number,
    max: number,
}